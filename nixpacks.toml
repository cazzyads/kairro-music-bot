[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.install]
cmds = [
    "which ffmpeg",
    "which ffprobe",
    "ffmpeg -version",
    "ffprobe -version",
    "pip install -r requirements.txt"
]

[start]
cmd = "python main.py"
